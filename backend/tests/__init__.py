"""
Test package for the CALC Scientific Calculator backend.

Contains unit tests for service modules and integration tests
for API endpoints. Uses pytest as the test runner with
mock MongoDB and mock Auth0 JWT for isolated testing.
"""
